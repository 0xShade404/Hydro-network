{
  "name": "@hydro/contracts-staking",
  "version": "0.1.0",
  "private": true,
  "description": "HYDRO staking rewards contract.",
  "scripts": {
    "compile": "ts-node --transpile-only scripts/compile.ts",
    "test": "npm run compile && hardhat test --no-compile",
    "node": "hardhat node",
    "deploy:local": "npm run compile && hardhat run scripts/deploy.ts --network localhost --no-compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "hardhat": "^2.22.5",
    "solc": "0.8.24",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
